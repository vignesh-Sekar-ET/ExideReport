import { AllowOnlyNumberDirective } from './allow-only-number.directive';

describe('AllowOnlyNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowOnlyNumberDirective();
    expect(directive).toBeTruthy();
  });
});
